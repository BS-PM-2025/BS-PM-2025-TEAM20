pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'jenkis', // או כל שם הסניף שברצונך לבנות
                    url: 'https://github.com/NaghmEbrahem30/Jenkins.git',
                    credentialsId: 'github-creds'
            }
        }

        stage('Run Django Tests') {
            steps {
                sh 'python3 manage.py test' // הרץ את מבחני Django
            }
        }
    }
}
